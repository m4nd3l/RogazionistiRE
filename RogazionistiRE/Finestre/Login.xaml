<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RogazionistiRE.Finestre.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RogazionistiRE.Finestre"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Login">
    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Title Bar -->
            <RowDefinition Height="Auto" />
            <!-- App Content -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar x:Name="LoginTitleBar"
              Title="RogazionistiRE - Login">
            <TitleBar.IconSource>
                <FontIconSource Glyph="&#xE8F1;"/>
            </TitleBar.IconSource>
        </TitleBar>

        <!-- App content -->
        <Frame x:Name="rootFrame" Grid.Row="1"/>

        <Button Content="premi qui per avviare la creazione della cartella" Click="creaCartella" 
                Grid.Row="2" Name="hello" Margin="0, -40, 0, 0"/>

    </Grid>

</Window>
