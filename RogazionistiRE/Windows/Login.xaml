<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RogazionistiRE.Windows.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RogazionistiRE.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Login">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="LoginTitleBar" Title="RogazionistiRE - Login">
            <TitleBar.IconSource>
                <FontIconSource Glyph="&#xE8F1;"/>
            </TitleBar.IconSource>
        </TitleBar>

        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Name="errors" Foreground="Red" Margin="0 0 0 10" />

            <TextBox Name="usernameBox" PlaceholderText="Username" Width="200" Height="32" Grid.Row="1" Margin="0 5"/>
            <PasswordBox Name="passwordBox" PlaceholderText="Password" Width="200" Height="32" Grid.Row="2" Margin="0 5"/>

            <CheckBox Name="rememberMe" Content="Remember Me" Grid.Row="3" Margin="0 5" HorizontalAlignment="Center"/>

            <Button Name="loginButton" Content="Login" Width="120" Height="32" Grid.Row="4" Margin="0 10" Click="login"/>
        </Grid>
    </Grid>
</Window>

