<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RogazionistiRE.Windows.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RogazionistiRE.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="{x:Bind TitleBarName()}">
            <TitleBar.IconSource>
                <FontIconSource Glyph="&#xE77B;"/>
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView Grid.Row="1" x:Name="navbar" 
                        SelectionChanged="NavViewSelection" 
                        BackRequested="BackToStudents"
                        IsBackEnabled="True"
                        IsSettingsVisible="False">
            
            <NavigationView.Header>
                <TextBlock Name="Title"
                           Text="Titolo"
                           FontSize="30"
                           Margin="12,0,0,12"
                           Style="{ThemeResource SubtitleTextBlockStyle}" />
            </NavigationView.Header>
            
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Home" Tag="Overview" Name="OverviewItem"/>
                <NavigationViewItem Content="Grades" Tag="Grades" Name="GradesItem">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE82D;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Icon="Calendar" Content="Agenda" Tag="Agenda" Name="AgendaItem"/>
                <NavigationViewItem Icon="ShowResults" Content="Absences" Tag="Absences" Name="AbsencesItem"/>
                <NavigationViewItem Content="Notes" Tag="Notes" Name="NotesItem">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEDB1;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Icon="Pin" Content="Communications" Tag="Communications" Name="CommunicationsItem"/>
                <NavigationViewItem Content="Report cards" Tag="ReportCards" Name="ReportCardsItem">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE9F9;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Subjects" Tag="Subjects" Name="SubjectsItem">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="More Info" Tag="Info" Name="MoreInfoItem">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF167;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Grid x:Name="ContentRoot">
                <Frame x:Name="ContentFrame"/>

                <Grid x:Name="ProgressOverlay" 
                      Visibility="Collapsed"
                      Opacity="0"
                      Canvas.ZIndex="100">
                    
                    <Grid.Resources>
                        <Storyboard x:Name="FadeInOverlay">
                            <DoubleAnimation Storyboard.TargetName="ProgressOverlay"
                                             Storyboard.TargetProperty="Opacity"
                                             From="0.0" To="1.0" Duration="0:0:0.2"/>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ProgressOverlay"
                                                           Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="Visible"/>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>

                        <Storyboard x:Name="FadeOutOverlay">
                            <DoubleAnimation Storyboard.TargetName="ProgressOverlay"
                                             Storyboard.TargetProperty="Opacity"
                                             From="1.0" To="0.0" Duration="0:0:0.2"/>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ProgressOverlay"
                                                           Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="Collapsed"/>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </Grid.Resources>

                    <Rectangle>
                        <Rectangle.Fill>
                            <AcrylicBrush AlwaysUseFallback="False" 
                                          TintOpacity="0.7" 
                                          TintColor="{ThemeResource SystemAltHighColor}"/>
                        </Rectangle.Fill>
                    </Rectangle>
                    
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16">
                        <ProgressRing IsActive="True" Width="48" Height="48"/>
                        <TextBlock x:Name="LoadingText" 
                                   Text="Loading..." 
                                   FontSize="16"
                                   FontWeight="Medium"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </NavigationView>
    </Grid>
</Page>